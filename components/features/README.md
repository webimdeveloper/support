# Feature Components

Feature-specific components belong here.

- Organize by domain (e.g. `admin/`, `client-dashboard/`).
- Compose from `components/ui` and `components/composed`.
- Avoid direct dependency on raw Nuxt UI primitives.
