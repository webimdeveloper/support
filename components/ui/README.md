# UI Wrappers

Public design-system wrappers live here.

- Use `Ui*` components as the only base primitives in feature code.
- Keep APIs stable; implementation may use Nuxt UI internally.
- New wrappers should support `variant` and `size` where applicable.
